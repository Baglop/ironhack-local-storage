import { StyleSheet } from 'react-native'
import { colors } from './colors'

export const taskCreationStyle = StyleSheet.create({
    container: {
        flex: 1,
        padding: 10,
        backgroundColor: colors.background
    }
})
